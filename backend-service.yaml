apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert --build local --profile gameprofile
    kompose.image-pull-policy: Never
    kompose.service.type: clusterip
    kompose.version: 1.36.0 (HEAD)
  labels:
    io.kompose.service: backend
  name: backend
spec:
  ports:
    - name: "8000"
      port: 8000
      targetPort: 8000
  selector:
    io.kompose.service: backend
  type: ClusterIP
